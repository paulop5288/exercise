package questions;

import java.util.HashSet;

public class HappyNumber {
    public static void main(String[] args) {
        HashSet<Simple> hashSet = new HashSet<>();
        hashSet.add(new Simple());
        hashSet.add(new Simple());

        System.out.println(hashSet);
    }
}
